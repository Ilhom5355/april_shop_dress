import 'package:flutter/material.dart';

class OnlyFavoritesPage extends StatelessWidget {
  const OnlyFavoritesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("data---2"),
      ),
    );
  }
}
